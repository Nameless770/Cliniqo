/**
 * Placeholder. The real dashboard is role-dependent: a receptionist sees today's
 * schedule and arrivals, a doctor sees their own list and unsigned notes, an
 * administrator sees staff and audit review.
 */
export default function DashboardPage() {
  return (
    <section>
      <h1 style={{ fontSize: '1.25rem', marginTop: 0 }}>Dashboard</h1>
      <p style={{ color: 'var(--color-muted)' }}>
        Scaffold only. Patient data is not wired up — phase 3.
      </p>
    </section>
  );
}
